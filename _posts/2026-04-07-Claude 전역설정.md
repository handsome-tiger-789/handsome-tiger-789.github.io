---
layout: post
title:  "Claude 전역설정"
date:   2026-04-07 11:00:00 +0900
category: [etc]
tags: [AI]
lastmod : 2026-04-07 11:00:00 +0900
changefreq : daily
priority : 1.0
---

## 최초 작성

`~/.claude/CLAUDE.md`

```md
# Global Development Guidelines

## Mandatory Pre-Edit Procedure

Before modifying any code, always present an execution plan in the format below
and wait for explicit user approval before proceeding.

### Plan Format
1. **Purpose**: What and why
2. **Files to change**: List of file paths
3. **Change summary**: What changes in each file
4. **Impact**: Potential side effects
5. **Rollback**: How to revert if something goes wrong

Never run Edit, Write, or file creation without prior approval.

## Tech Stack
- Backend: Java / Spring Boot
  - Java version: check `.java-version` or `pom.xml <java.version>`
  - Spring Boot version: check `pom.xml <parent>` block
- Frontend: Node.js / React
  - Node version: check `.nvmrc` or `package.json engines` field
  - Package versions: always read `package.json` before suggesting changes
- Build tools: Maven / Gradle
- Dependencies: Always use exact versions from pom.xml/package.json

## Coding Conventions

### Comments (Required)
- Add JSDoc/JavaDoc to all public methods/classes
- Inline comments for complex logic (10+ lines)
- Include version + reason when adding dependencies

### Exception Handling
- Use unchecked exceptions for programming errors
- Define custom exception hierarchy under a base `ApplicationException`
- Never swallow exceptions silently; always log with sufficient context

### Logging (SLF4J)
- ERROR: system failures requiring immediate attention
- WARN: recoverable issues or unexpected but non-fatal states
- INFO: business-significant events only (not chatty)
- DEBUG: development/troubleshooting only, never in hot paths
- Use MDC for request tracing (requestId, userId at minimum)

### Testing
- Unit tests: service layer and below; mock external dependencies
- Integration tests: repository layer; use `@SpringBootTest` sparingly
- Target 80%+ coverage on service layer
- Test method naming: `methodName_scenario_expectedResult`

### API Design
- REST response envelope: `{ data, error, meta }`
- Error codes: domain-prefixed (e.g. `USER_NOT_FOUND`, `ORDER_INVALID_STATE`)
- Validate all inputs at controller boundary using `@Valid` + custom validators
- Never expose internal stack traces in API responses

### Dependency Management
- Specify exact versions in pom.xml/package.json
- Never use "latest"
- Include change reason + test results in Plan

## Git / Deployment
- Do NOT run git commit, push, checkout, add, or stash. User handles all git operations.
  - `git stash` is also blocked: stash operations can silently discard in-progress work.
- Do NOT run Docker commands. User handles all deployment operations.

## Response Style
- Be concise. Avoid redundant explanations.
- Do not list multiple alternatives unless explicitly asked.
- Always include relevant comments with version information.
```

> **Mandatory Pre-Edit Procedure** : 코드 수정 전 → 실행 계획 제시 → 사용자 명시적 승인 → 편집 실행<br/>
> **Plan Format** : 1. 목적+이유 / 2. 수정할 파일 경로 목록 / 3. 각 파일별 변경 내용 / 4. 부작용 가능성 / 5. 롤백 방법<br/>
> 승인 없이는 절대 파일 수정 금지<br/>
> **Coding Conventions** : 예외처리 계층 / SLF4J 로그 레벨 기준 / 테스트 전략 및 커버리지 / REST API 응답 포맷<br/>
> **Git / Deployment** : git stash 포함 모든 Git 쓰기 명령 및 Docker 금지<br/>
> **Response Style** : 응답은 간결하게, 대안 여러개 제시 금지 (명시적 요청시에만) → 토큰 아끼기


<br/><br/>

`~/.claude/settings.json`

```json
{
  "env": {
    "JAVA_TOOL_OPTIONS": "-Dfile.encoding=UTF-8"
  },
  "permissions": {
    "allow": [
      "Bash(./mvnw *)",
      "Bash(mvn *)",
      "Bash(mvn --version)",
      "Bash(./gradlew *)",
      "Bash(gradle *)",
      "Bash(gradle --version)",
      "Bash(git status)",
      "Bash(git diff *)",
      "Bash(git log *)",
      "Bash(git branch *)",
      "Bash(java -version)",
      "Bash(npm install*)",
      "Bash(npm run *)",
      "Bash(npm test)",
      "Bash(npm ci)",
      "Bash(npm --version)",
      "Bash(npm ls *)",
      "Bash(npx *)",
      "Bash(node *)",
      "Bash(node --version)",
      "Bash(curl http://localhost*)",
      "Bash(curl http://127.0.0.1*)",
      "Bash(ps *)",
      "Read",
      "Edit(*.java)",
      "Edit(*.js)",
      "Edit(*.ts)",
      "Edit(*.tsx)",
      "Edit(*.jsx)",
      "Edit(*.xml)",
      "Edit(*.json)",
      "Edit(*.sql)",
      "Edit(*.sh)",
      "Edit(*.kts)",
      "Edit(*.gradle)",
      "Edit(*.yaml)",
      "Edit(Dockerfile)",
      "Edit(.gitignore)",
      "Edit(Makefile)",
      "Write(*.java|*.js|*.ts|*.tsx|*.jsx|*.xml|*.json|*.sql|*.sh|*.kts|*.gradle|*.yaml)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git push *)",
      "Bash(git checkout *)",
      "Bash(git stash *)",
      "Bash(docker *)",
      "Bash(npm publish*)",
      "Bash(npm login*)",
      "Bash(npm adduser*)",
      "Bash(npm token*)",
      "Edit(.env*)",
      "Edit(*.properties)",
      "Edit(docker-compose*.yml)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(echo \"$CLAUDE_TOOL_INPUT\" | jq -r '.command' 2>/dev/null || echo ''); if [ -z \"$cmd\" ]; then exit 0; fi; if echo \"$cmd\" | grep -qE '(rm -rf|DROP TABLE|DELETE FROM)'; then echo \"[hook] Blocked dangerous command: $cmd\" >&2; exit 1; fi; if echo \"$cmd\" | grep -qP 'curl.+(-d|--data|--data-binary|--upload-file).+https?://(?!(localhost|127\\.0\\.0\\.1))'; then echo \"[hook] Blocked: curl external data transfer\" >&2; exit 1; fi"
          }
        ]
      }
    ]
  }
}
```

> **JAVA_TOOL_OPTIONS** : UTF-8 인코딩 강제 / 한글 깨짐 예방<br/>
> **허용** :<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;빌드/테스트 : Maven, Gradle<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;Git 읽기 전용 : status, diff, log, branch<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;Node/npm : install, run, test, ci, ls (배포/인증 명령 제외)<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;curl : localhost/127.0.0.1 한정<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;파일 편집 : .java .js .ts .tsx .jsx .xml .json .sql .sh .kts .gradle .yaml Dockerfile .gitignore Makefile<br/>
> **차단** :<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;삭제 : rm -rf<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;Git 쓰기 : add, commit, push, checkout, stash<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;Docker 전체<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;npm 배포/인증 : publish, login, adduser, token<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;민감 파일 편집 : .env, .properties, docker-compose*.yml<br/>
> **hooks.PreToolUse** :<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;rm -rf / DROP TABLE / DELETE FROM 포함 명령 차단<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;curl로 외부 서버에 데이터 전송 시 차단 (localhost 제외)<br/>
> &nbsp;&nbsp;&nbsp;&nbsp;jq 미설치 시 fallback 처리, 차단 시 stderr 메시지 출력

<br/>

---

## 2026-05-24 수정

`~/.claude/settings.json` 수정 <br/>

변경 요약 : 

| # | 항목               | Before                                                                       | After                                                                                                                                                                                                                               | 효과                                        |
|---|------------------|------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------|
| A | Write 권한 패턴      | `Write(*.java \| *.js\| *.ts \|...)` 한 줄 (파이프 OR — 미동작)                      | 확장자별 13개 라인으로 분리 `(Write(*.java), Write(*.js) …)` + \*.yml 추가                                                                                                                                                                       | 실제로 매칭되어 저장 시 prompt 안 뜸                  |
| B   | curl localhost 패턴 |`Bash(curl http://localhost*) / 127.0.0.1*`                                   | `Bash(curl http://localhost:*)`, `Bash(curl http://localhost/*)`, `Bash(curl http://127.0.0.1:*)`, `Bash(curl http://127.0.0.1/*)`                                                                                                  | localhost.evil.com 같은 prefix 우회 차단        |
| C-1 | Hook 입력 읽기  | $CLAUDE_TOOL_INPUT 환경변수 (현행 사양 없음 — 항상 통과)                                   | `input=$(cat) + jq -r '.tool_input.command' `(stdin JSON)                                                                                                                                                                           | hook이 실제로 동작                              |
| C-2 | Hook 차단 키워드  | rm -rf, DROP TABLE, DELETE FROM                                              | + TRUNCATE TABLE 추가                                                                                                                                                                                                                 | MySQL 작업 중 사고 방지                          |
| C-3 | Hook exit code       | exit 1                                                                       | exit 2                                                                                                                                                                                                                              | Claude Code 규약상 2가 "차단 + 사유 전달". 1은 단순 실패 |
| C-4 | Hook curl 외부전송  | `grep -qP '…(?!(localhost\|127\.0\.0\.1))'`  (PCRE lookahead — macOS BSD grep 미지원) | `grep -qE '…://'` 1차 매칭 후 `! grep -qE 'localhost\|127\.0\.0\.1'` 2단계                                                                                                                                                                | macOS에서 외부 업로드 실제 차단                      |
| D   | 민감 파일 deny 추가   | (없음)                                                                         | `Edit(**/credentials*.json)`, `Edit(**/secrets*.json)`, `Edit(**/*-secret.json)`, `Edit(**/firebase-adminsdk*.json)`, `Edit(**/serviceAccountKey*.json)`, `Edit(**/.npmrc)`, `Edit(**/id_rsa*)`, `Edit(**/*.pem)`, `Edit(**/*.key)` | 자격증명/키 파일 편집 차단. 일반 \*.json은 영향 없음        |


```md
 {
   "env": {
     "JAVA_TOOL_OPTIONS": "-Dfile.encoding=UTF-8"
   },
   "permissions": {
     "allow": [
       "Bash(./mvnw *)",
       "Bash(mvn *)",
       "Bash(mvn --version)",
       "Bash(./gradlew *)",
       "Bash(gradle *)",
       "Bash(gradle --version)",
       "Bash(git status)",
       "Bash(git diff *)",
       "Bash(git log *)",
       "Bash(git branch *)",
       "Bash(java -version)",
       "Bash(npm install*)",
       "Bash(npm run *)",
       "Bash(npm test)",
       "Bash(npm ci)",
       "Bash(npm --version)",
       "Bash(npm ls *)",
       "Bash(npx *)",
       "Bash(node *)",
       "Bash(node --version)",
       "Bash(curl http://localhost:*)",
       "Bash(curl http://localhost/*)",
       "Bash(curl http://127.0.0.1:*)",
       "Bash(curl http://127.0.0.1/*)",
       "Bash(ps *)",
       "Read",
       "Edit(*.java)",
       "Edit(*.js)",
       "Edit(*.ts)",
       "Edit(*.tsx)",
       "Edit(*.jsx)",
       "Edit(*.xml)",
       "Edit(*.json)",
       "Edit(*.sql)",
       "Edit(*.sh)",
       "Edit(*.kts)",
       "Edit(*.gradle)",
       "Edit(*.yaml)",
       "Edit(Dockerfile)",
       "Edit(.gitignore)",
       "Edit(Makefile)",
       "Write(*.java)",
       "Write(*.js)",
       "Write(*.ts)",
       "Write(*.tsx)",
       "Write(*.jsx)",
       "Write(*.xml)",
       "Write(*.json)",
       "Write(*.sql)",
       "Write(*.sh)",
       "Write(*.kts)",
       "Write(*.gradle)",
       "Write(*.yaml)",
 "Write(*.yml)"
     ],
     "deny": [
       "Bash(rm -rf *)",
       "Bash(git add *)",
       "Bash(git commit *)",
       "Bash(git push *)",
       "Bash(git checkout *)",
       "Bash(git stash *)",
       "Bash(docker *)",
       "Bash(npm publish*)",
       "Bash(npm login*)",
       "Bash(npm adduser*)",
       "Bash(npm token*)",
       "Edit(.env*)",
       "Edit(*.properties)",
       "Edit(docker-compose*.yml)",
       "Edit(**/credentials*.json)",
       "Edit(**/secrets*.json)",
       "Edit(**/*-secret.json)",
       "Edit(**/firebase-adminsdk*.json)",
       "Edit(**/serviceAccountKey*.json)",
       "Edit(**/.npmrc)",
       "Edit(**/id_rsa*)",
       "Edit(**/*.pem)",
 "Edit(**/*.key)"
     ]
   },
   "hooks": {
     "PreToolUse": [
       {
         "matcher": "Bash",
         "hooks": [
           {
             "type": "command",
             "command": "input=$(cat); cmd=$(echo \"$input\" | jq -r '.tool_input.command // empty' 2>/dev/null); if [ -z \"$cmd\" ]; then exit 0; fi; if echo \"$cmd\" | grep -qE
  '(rm -rf|DROP TABLE|DELETE FROM|TRUNCATE TABLE)'; then echo \"[hook] Blocked dangerous command: $cmd\" >&2; exit 2; fi; if echo \"$cmd\" | grep -qE
 'curl.+(-d|--data|--data-binary|--upload-file).+https?://'; then if ! echo \"$cmd\" | grep -qE 'https?://(localhost|127\\.0\\.0\\.1)'; then echo \"[hook] Blocked: curl external
 data transfer\" >&2; exit 2; fi; fi"
           }
         ]
       }
     ]
   },
   "statusLine": {
     "type": "command",
     "command": "/Users/pm1145/.claude/statusline.sh",
     "padding": 0
   }
 }
```
<br/>

`~/.claude/CLAUDE.md` 수정 <br/>

변경 요약:

|섹션| 적용내용                                                                   |
|--|------------------------------------------------------------------------|
|B Tech Stack   | Java 17+ / Spring Boot 4.x, MySQL primary 명시, Frontend는 occasional로 강등 |
|C Comments         | "모든 public에 JavaDoc" → "공개 API에만 + WHY가 비자명할 때만". WHAT 코멘트 금지          |
| C Logging          |  MDC에 traceId 추가 (Spring Boot 4.x Micrometer Tracing 기본키)              |
| D Testing          | 80% 강제 제거. MySQL Testcontainers 권장 추가                                    |
| F Dependency       | Spring Boot BOM 위임 한 줄 추가                                                |
| G Git/Deployment   | 명령 나열 → settings.json deny 참조 한 줄                                        |
| H Response Style   | "Always include relevant comments..." 줄 삭제 (Coding/Dependency 룰로 커버됨)    |
| I MySQL Operations | 새 섹션 추가 (마이그레이션 분리, WHERE 강제, utf8mb4, 대용량 ALTER 경고)         |

```md
 # Global Development Guidelines

 ## Mandatory Pre-Edit Procedure

 Before modifying any code, always present an execution plan in the format below
 and wait for explicit user approval before proceeding.

 ### Plan Format
 1. **Purpose**: What and why
 2. **Files to change**: List of file paths
 3. **Change summary**: What changes in each file
 4. **Impact**: Potential side effects
 5. **Rollback**: How to revert if something goes wrong

 Never run Edit, Write, or file creation without prior approval.

 ## Tech Stack
 - Backend (primary): Java 17+ / Spring Boot 4.x
   - Java version: check `.java-version` or `pom.xml <java.version>`
   - Spring Boot version: check `pom.xml <parent>` block
 - Database (primary): MySQL
 - Frontend (occasional): Node.js / React
   - Node version: check `.nvmrc` or `package.json engines` field
   - Detailed frontend conventions belong in project-level CLAUDE.md
 - Build tools: Maven / Gradle
 - Dependencies: always read pom.xml/package.json before suggesting changes

 ## Coding Conventions

 ### Comments
 - JavaDoc/JSDoc only for public APIs (signatures imported by other modules)
 - Otherwise, write a comment only when the WHY is non-obvious
 - Do not write WHAT comments (well-named identifiers cover that)
 - Include version + reason when adding dependencies

 ### Exception Handling
 - Use unchecked exceptions for programming errors
 - Define custom exception hierarchy under a base `ApplicationException`
 - Never swallow exceptions silently; always log with sufficient context

 ### Logging (SLF4J)
 - ERROR: system failures requiring immediate attention
 - WARN: recoverable issues or unexpected but non-fatal states
 - INFO: business-significant events only (not chatty)
 - DEBUG: development/troubleshooting only, never in hot paths
 - Use MDC for request tracing (at minimum: traceId, requestId, userId)

 ### Testing
 - Unit tests: service layer and below; mock external dependencies
 - Integration tests: repository layer; use `@SpringBootTest` sparingly
 - Coverage: prioritize core business logic (service/domain). Follow project standard if defined; do not enforce a global
 percentage.
 - MySQL integration tests: use Testcontainers MySQL, not H2 (dialect drift causes prod surprises)
 - Test method naming: `methodName_scenario_expectedResult`

 ### API Design
 - REST response envelope: `{ data, error, meta }`
 - Error codes: domain-prefixed (e.g. `USER_NOT_FOUND`, `ORDER_INVALID_STATE`)
 - Validate all inputs at controller boundary using `@Valid` + custom validators
 - Never expose internal stack traces in API responses

 ### Dependency Management
 - Specify exact versions in pom.xml/package.json
 - Never use "latest"
 - Spring Boot starters: rely on the BOM-managed version; pin explicit version only for libraries not in the BOM
 - Include change reason + test results in Plan

 ## MySQL Operations
 - Migrations always in dedicated files (Flyway/Liquibase). No inline DDL in application code.
 - `DELETE` / `UPDATE` must include `WHERE`. Warn loudly if a query lacks one.
 - Assume `utf8mb4` charset. Report `latin1` or other non-utf8 collations immediately.
 - Index changes and `ALTER` on large tables are treated as non-trivial changes (Pre-Edit Procedure applies).

 ## Git / Deployment
 - git / docker / deploy commands are blocked via settings.json `deny`. The user handles these directly.
 - Do not attempt blocked commands; report results to the user and let them run the command.

 ## Response Style
 - Be concise. Avoid redundant explanations.
 - Do not list multiple alternatives unless explicitly asked.
```

<br>
## 2026-06-13 수정

`~/.claude/CLAUDE.md`
<br/>
변경요약 <br/>
>[OMC](https://github.com/Yeachan-Heo/oh-my-claudecode){:target="_blank"} 서브에이전트/자율모드 사용을 위해 기존  금지 조항과 충돌하는 부분 수정<br/>
>`~/.claude/CLAUDE-omc.md` 파일이 신규 생성되지만 <br/>
>`@CLAUDE-omc.md import` 구문이 추가되기 때문에 기존 금지 조항과 충돌 <br/>
> 대화모드에선 기존 금지조항을 준수

| 섹션                                                                                                | 적용내용                                                                                                                                                                                                                                                                                                                                                                                                                                           |
|---------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Pre-Edit 안내 문구                                                                                    | "always present an execution plan" → "present an execution plan" (always 삭제, 면제 도입의 전제)                                                                                                                                                                                                                                                                                                                                                        |
| Plan Format                                                                                       | 5단계(Purpose/Files/Summary/Impact/Rollback) 그대로 유지                                                                                                                                                                                                                                                                                                                                                                                              |
| Exemptions (신규)                                                                                   | 무조건 승인 → 3가지 면제 도입: ① 서브에이전트, ② OMC 자율 모드, ③ 세션 부기 파일 (명시적 화이트리스트)                                                                                                                                                                                                                                                                                                                                                                             |
| ① Sub-agent 면제                                                                                    | Agent 툴로 호출 시 부모가 이미 승인 → 재질문 금지, 결과만 반환                                                                                                                                                                                                                                                                                                                                                                                                       |
| ② OMC 자율 모드 면제                                                                                    | /autopilot, /ralph, /ultrawork, /team, /ralplan 호출 자체가 승인                                                                                                                                                                                                                                                                                                                                                                                      |
| ③ 세션 부기 파일 면제 (명시적 화이트리스트)                                                                        | Claude Code 세션 상태: `projects/**`, `sessions/**`, `todos/**`, `tasks/**`, <br>`file-history/**`, `shell-snapshots/**`, `cache/**`, `statsig/**`, `telemetry/**`, <br>`paste-cache/**`, `image-cache/**`, `debug/**`, `downloads/**` <br> - 플러그인 캐시: `~/.claude/plugins/cache/**` <br> - OMC 세션 상태:  `.omc/state/**`, `.omc/sessions/**`, `.omc/notepad.md`, `.omc/research/**`, <br>`.omc/logs/**`, `.omc/plans/**`, `.omc/project-memory.json` |
| 와일드카드 차단 명시                                                                                       | `~/.claude/**`, `.claude/**` 같은 통째 면제 금지 단서 추가 <br>("No blanket exemption")                                                                                                                                                                                                                                                                                                                                                                    |
| Hard Rule – Code & data 작업                                                                        | 5종 유지: 파괴적 git(force push/reset --hard/branch -D), 운영 DDL/DML(DROP/TRUNCATE/대량 UPDATE·DELETE), <br>의존성 버전 변경(`pom.xml`/`package.json`/`lock`), <br>CI/CD·시크릿·자격증명·환경변수, 외부 side-effect(`git push`/`PR`/`Slack`/`email`/`webhook`)                                                                                                                                                                                                              |
| Hard Rule – Agent behavior & permission files <br>(신규 8종 추가)                                      | 행동 정의: CLAUDE.md, CLAUDE-omc.md, AGENTS.md, 프로젝트 .claude/CLAUDE.md <br>- 권한·실행 제어: ~/.claude/settings.json, settings.local.json, keybindings.json<br>- 훅: ~/.claude/hooks/** + settings.json의 hooks 필드<br>- statusline: ~/.claude/statusline.sh <br>- 자격증명: ~/.claude/.credentials.json<br>- 커스텀 자산: `~/.claude/agents/**`, `skills/**`, `commands/**`<br>- MCP: .mcp.json, ~/.claude.json mcpServers<br>- OMC 설정: ~/.claude/.omc-config.json  |
| 마지막 문장                                                                                            | "Never run Edit/Write without prior approval" → "outside the above exemptions" 단서 추가 <br><br>(면제조항에 해당하지 않을 경우에만 금지 규칙 적용되도록)                                                                                                                                                                                                                                                                                                                  |
| 그 외 섹션 <br>(Tech Stack, Coding Conventions, <br>MySQL Operations, Git/Deployment, Response Style) | 변경 X                                                                                                                                                                                                                                                                                                                                                                                                                                           |

```md
# Global Development Guidelines

## Mandatory Pre-Edit Procedure

Before modifying any code, present an execution plan in the format below
and wait for explicit user approval before proceeding.

### Plan Format
1. **Purpose**: What and why
2. **Files to change**: List of file paths
3. **Change summary**: What changes in each file
4. **Impact**: Potential side effects
5. **Rollback**: How to revert if something goes wrong

### Exemptions (no plan required)
Skip the plan only for these — approval was granted at a higher level, or
the file is pure session bookkeeping with no behavioral impact:

1. **Sub-agent execution** — When invoked via the Agent tool, the parent
   session already obtained user approval for the task scope. Proceed
   without re-prompting; return results to the parent.
2. **OMC autonomous modes** — When the user has invoked `/autopilot`,
   `/ralph`, `/ultrawork`, `/team`, `/ralplan`, or similar tier-0 OMC
   skills, the orchestration command itself is the approval. The skill's
   own plan-then-execute step replaces per-edit approval.
3. **Session bookkeeping files only** (explicit whitelist):
   - Claude Code session state:
     `~/.claude/projects/**`, `sessions/**`, `todos/**`, `tasks/**`,
     `file-history/**`, `shell-snapshots/**`, `cache/**`, `statsig/**`,
     `telemetry/**`, `paste-cache/**`, `image-cache/**`, `debug/**`,
     `downloads/**`
   - Plugin cache (managed by plugin system):
     `~/.claude/plugins/cache/**`
   - OMC session state:
     `.omc/state/**`, `.omc/sessions/**`, `.omc/notepad.md`,
     `.omc/research/**`, `.omc/logs/**`, `.omc/plans/**`,
     `.omc/project-memory.json`

   No blanket `~/.claude/**` or `.claude/**` exemption. Anything not
   explicitly listed above requires approval (see Hard Rule below).

### Hard rule (no exemption, ever)
Even inside an autonomous mode or sub-agent, ALWAYS pause and ask the user
before:

**Code & data operations**
- Destructive git operations: force push, `reset --hard`, `branch -D`,
  rewriting published history
- Database DDL/DML on prod-shaped data: `DROP`, `TRUNCATE`, mass
  `UPDATE`/`DELETE`, schema-altering migrations on large tables
- Dependency version changes in `pom.xml` / `package.json` /
  `package-lock.json`
- Modifying CI/CD pipelines, secrets, credentials, environment variables
- External side-effects: `git push`, PR creation/comment, Slack, email,
  webhook calls

**Agent behavior & permission files**
- Behavior definition: `CLAUDE.md`, `CLAUDE-omc.md`, `AGENTS.md`,
  project-level `.claude/CLAUDE.md`
- Permissions / execution control: `~/.claude/settings.json`,
  `settings.local.json`, `keybindings.json`
- Hooks: `~/.claude/hooks/**`, and the `hooks` field inside `settings.json`
- Statusline: `~/.claude/statusline.sh` and any executable statusline script
- Credentials: `~/.claude/.credentials.json`
- Custom agents / skills / commands: `~/.claude/agents/**`, `skills/**`,
  `commands/**`
- MCP server registration: `.mcp.json`, the `mcpServers` section in
  `~/.claude.json`
- OMC configuration: `~/.claude/.omc-config.json`

Never run Edit, Write, or file creation outside the above exemptions
without prior approval.

## Tech Stack
- Backend (primary): Java 17+ / Spring Boot 4.x
  - Java version: check `.java-version` or `pom.xml <java.version>`
  - Spring Boot version: check `pom.xml <parent>` block
- Database (primary): MySQL
- Frontend (occasional): Node.js / React
  - Node version: check `.nvmrc` or `package.json engines` field
  - Detailed frontend conventions belong in project-level CLAUDE.md
- Build tools: Maven / Gradle
- Dependencies: always read pom.xml/package.json before suggesting changes

## Coding Conventions

### Comments
- JavaDoc/JSDoc only for public APIs (signatures imported by other modules)
- Otherwise, write a comment only when the WHY is non-obvious
- Do not write WHAT comments (well-named identifiers cover that)
- Include version + reason when adding dependencies

### Exception Handling
- Use unchecked exceptions for programming errors
- Define custom exception hierarchy under a base `ApplicationException`
- Never swallow exceptions silently; always log with sufficient context

### Logging (SLF4J)
- ERROR: system failures requiring immediate attention
- WARN: recoverable issues or unexpected but non-fatal states
- INFO: business-significant events only (not chatty)
- DEBUG: development/troubleshooting only, never in hot paths
- Use MDC for request tracing (at minimum: traceId, requestId, userId)

### Testing
- Unit tests: service layer and below; mock external dependencies
- Integration tests: repository layer; use `@SpringBootTest` sparingly
- Coverage: prioritize core business logic (service/domain). Follow project standard if defined; do not enforce a global percentage.
- MySQL integration tests: use Testcontainers MySQL, not H2 (dialect drift causes prod surprises)
- Test method naming: `methodName_scenario_expectedResult`

### API Design
- REST response envelope: `{ data, error, meta }`
- Error codes: domain-prefixed (e.g. `USER_NOT_FOUND`, `ORDER_INVALID_STATE`)
- Validate all inputs at controller boundary using `@Valid` + custom validators
- Never expose internal stack traces in API responses

### Dependency Management
- Specify exact versions in pom.xml/package.json
- Never use "latest"
- Spring Boot starters: rely on the BOM-managed version; pin explicit version only for libraries not in the BOM
- Include change reason + test results in Plan

## MySQL Operations
- Migrations always in dedicated files (Flyway/Liquibase). No inline DDL in application code.
- `DELETE` / `UPDATE` must include `WHERE`. Warn loudly if a query lacks one.
- Assume `utf8mb4` charset. Report `latin1` or other non-utf8 collations immediately.
- Index changes and `ALTER` on large tables are treated as non-trivial changes (Pre-Edit Procedure applies).

## Git / Deployment
- git / docker / deploy commands are blocked via settings.json `deny`. The user handles these directly.
- Do not attempt blocked commands; report results to the user and let them run the command.

## Response Style
- Be concise. Avoid redundant explanations.
- Do not list multiple alternatives unless explicitly asked.
```

<br><br>
`~/.claude/settings.json` <br>변경요약 <br>
>settings.json 권한 정리 (2026-06-14): allow 는 Bash/Edit/Write/Read 와일드카드로 단순화해 일상 1/2/3 프롬프트 제거, deny 는 자격증명·파괴 명령 위주로 34건 추가.

\[allow\] 와일드카드로 통합<br>
`Bash(*), Edit(*), Write(*), Read, mcp__*` <br>
→ 기존 38개 구체 패턴 삭제. 일반 개발 명령 및 mcp 무프롬프트. <br>                         
\[deny\] 강화 - 1/2/3 프롬프트 추가 없이 안전망만 +
-  Bash 시스템: `sudo, chown, chmod 777, dd, mkfs, curl|sh, wget|sh`
-  인프라 파괴: `kubectl delete, aws s3 rm/rb, aws iam, terraform destroy, gh repo/release delete`
-  Write 자격증명 (Edit 와 대칭): `.env*, *.pem, id_rsa, credentials/secrets JSON, .npmrc`
-  Read 자격증명: `~/.ssh/**, ~/.aws/**, ~/.gnupg/**, .credentials.json, *.pem, *.key, .env*`
   <br><br>

\[유지\] 기존 정책
-  `git add/commit/push/checkout/stash, docker, npm publish` <br>→ 자동 실행 차단, 본인이 직접
-  PreToolUse 훅: rm -rf, DROP/DELETE/TRUNCATE, 외부 URL POST 차단
   <br><br>

\[효과\]
- 일상 업무 1/2/3 프롬프트 거의 사라짐
-  자격증명 읽기/쓰기 + 인프라 파괴는 시스템 레벨 차단

```json
{
  "env": {
    "JAVA_TOOL_OPTIONS": "-Dfile.encoding=UTF-8"
  },
  "permissions": {
    "allow": [
      "Bash(*)",
      "Edit(*)",
      "Write(*)",
      "Read",
      "mcp__*"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git push *)",
      "Bash(git checkout *)",
      "Bash(git stash *)",
      "Bash(docker *)",
      "Bash(npm publish*)",
      "Bash(npm login*)",
      "Bash(npm adduser*)",
      "Bash(npm token*)",
      "Bash(sudo *)",
      "Bash(chown *)",
      "Bash(chmod 777 *)",
      "Bash(dd *)",
      "Bash(mkfs*)",
      "Bash(curl * | sh)",
      "Bash(curl * | bash)",
      "Bash(wget * | sh)",
      "Bash(wget * | bash)",
      "Bash(kubectl delete *)",
      "Bash(aws s3 rm *)",
      "Bash(aws s3 rb *)",
      "Bash(aws iam *)",
      "Bash(terraform destroy*)",
      "Bash(gh repo delete *)",
      "Bash(gh release delete *)",
      "Edit(.env*)",
      "Edit(*.properties)",
      "Edit(docker-compose*.yml)",
      "Edit(**/credentials*.json)",
      "Edit(**/secrets*.json)",
      "Edit(**/*-secret.json)",
      "Edit(**/firebase-adminsdk*.json)",
      "Edit(**/serviceAccountKey*.json)",
      "Edit(**/.npmrc)",
      "Edit(**/id_rsa*)",
      "Edit(**/*.pem)",
      "Edit(**/*.key)",
      "Write(.env*)",
      "Write(*.properties)",
      "Write(**/credentials*.json)",
      "Write(**/secrets*.json)",
      "Write(**/*-secret.json)",
      "Write(**/firebase-adminsdk*.json)",
      "Write(**/serviceAccountKey*.json)",
      "Write(**/.npmrc)",
      "Write(**/id_rsa*)",
      "Write(**/*.pem)",
      "Write(**/*.key)",
      "Read(~/.ssh/**)",
      "Read(~/.aws/**)",
      "Read(~/.gnupg/**)",
      "Read(~/.claude/.credentials.json)",
      "Read(**/id_rsa*)",
      "Read(**/*.pem)",
      "Read(**/*.key)",
      "Read(**/.env*)",
      "Read(**/credentials*.json)",
      "Read(**/secrets*.json)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); cmd=$(echo \"$input\" | jq -r '.tool_input.command // empty' 2>/dev/null); if [ -z \"$cmd\" ]; then exit 0; fi; if echo \"$cmd\" | grep -qE '(rm -rf|DROP TABLE|DELETE FROM|TRUNCATE TABLE)'; then echo \"[hook] Blocked dangerous command: $cmd\" >&2; exit 2; fi; if echo \"$cmd\" | grep -qE 'curl.+(-d|--data|--data-binary|--upload-file).+https?://'; then if ! echo \"$cmd\" | grep -qE 'https?://(localhost|127\\.0\\.0\\.1)'; then echo \"[hook] Blocked: curl external data transfer\" >&2; exit 2; fi; fi"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "/Users/pm1145/.claude/statusline-combined.sh",
    "padding": 0
  }
}
```